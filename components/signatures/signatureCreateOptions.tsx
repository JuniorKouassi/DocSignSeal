/* Shared between SignatureFab (mobile) and SignatureMenu (desktop) so the
   two destinations can't drift apart. */
export const DRAW_HREF = '/dashboard/signatures/new/draw';
export const TYPE_HREF = '/dashboard/signatures/new/type';
